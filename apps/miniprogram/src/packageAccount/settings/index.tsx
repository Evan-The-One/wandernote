import Taro from"@tarojs/taro";import{Button,View}from"@tarojs/components";import{logout}from"../../services/session";
export default function Settings(){async function signOut(){const ok=await Taro.showModal({title:"退出当前账号？",content:"退出不会删除攻略、点数或海报。",confirmText:"退出"});if(!ok.confirm)return;await logout();await Taro.reLaunch({url:"/pages/start/index"})}return <View className="page"><View className="title">账户设置</View><View className="card"><Button className="secondary" onClick={signOut}>退出登录</Button><Button className="secondary" onClick={()=>Taro.navigateTo({url:"/packageAccount/legal/index?action=delete"})}>申请注销账户</Button></View></View>}
