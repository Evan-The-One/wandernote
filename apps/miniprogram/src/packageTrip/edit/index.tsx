import {useState}from"react";import Taro,{useRouter}from"@tarojs/taro";import{Button,Textarea,View}from"@tarojs/components";import{request}from"../../services/session";
export default function Edit(){const p=useRouter().params,[text,setText]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false);async function submit(){if(text.trim().length<2){setError("请简单说说想怎么调整");return}setLoading(true);setError("");try{await request(`/api/miniapp/trips/${p.tripId}/revise`,{method:"POST",data:{version:Number(p.version),dayNumber:Number(p.day),instruction:text.trim()}});await Taro.showToast({title:"这一天已调整",icon:"success"});setTimeout(()=>Taro.navigateBack(),600)}catch(e){setError(e instanceof Error?e.message:"暂时没能调整")}finally{setLoading(false)}}return <View className="page"><View className="title">怎么调整这一天？</View><Textarea className="input textarea" maxlength={500} value={text} onInput={e=>setText(e.detail.value)} placeholder="例如：下午少走一点，留时间休息"/>{error&&<View className="error">{error}</View>}<Button className="primary" loading={loading} disabled={loading} onClick={submit}>{loading?"正在重新安排…":"确认调整"}</Button><Button className="secondary" onClick={()=>Taro.navigateBack()}>返回行程</Button></View>}
