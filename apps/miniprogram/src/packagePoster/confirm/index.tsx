import Taro,{useRouter}from"@tarojs/taro";import{Button,View}from"@tarojs/components";
export default function Confirm(){const p=useRouter().params,days=Number(p.days||1),pages=Math.ceil(days/2);return <View className="page"><View className="title">生成你的旅行海报</View><View className="card"><View>预计 {pages} 张</View><View>预计消耗 {pages} 点</View><View className="muted">海报会根据当前行程生成，路线需要调整的话可以先返回。</View></View><Button className="secondary" onClick={()=>Taro.navigateTo({url:"/packagePoster/examples/index"})}>先看示例</Button><Button className="primary" onClick={()=>Taro.navigateTo({url:`/packagePoster/progress/index?tripId=${p.tripId}`})}>确认生成海报</Button></View>}
