import type { TripInput } from "@/types/trip";
import { estimateIntercityTransport } from "./intercity-transport";
export function IntercitySummary({input}:{input:TripInput}){const value=estimateIntercityTransport(input);if(!value)return null;return <section className="card rounded-3xl p-4 sm:p-7"><h2 className="text-lg font-bold">出发与返程</h2><div className="mt-4 grid grid-cols-2 gap-2 sm:gap-3"><TripLeg title="出发" route={`${value.from} → ${value.to}`} recommendation={value.recommendation} duration={value.duration} advice={value.departureImpact}/><TripLeg title="返程" route={`${value.to} → ${value.from}`} recommendation={value.recommendation} duration={value.duration} advice={value.returnImpact}/></div><p className="mt-3 text-xs text-[#707a74]">交通时间为大致估算，出发前请核实实际路况或班次。</p></section>}
function TripLeg({title,route,recommendation,duration,advice}:{title:string;route:string;recommendation:string;duration:string;advice:string}){return <div className="min-w-0 rounded-2xl bg-[#f4f6f1] p-3 sm:p-4"><p className="text-xs font-bold text-[#287057]">{title}</p><strong className="mt-1 block break-words text-sm sm:text-base">{route}</strong><p className="mt-2 text-xs font-semibold text-[#526159] sm:text-sm">{recommendation}</p><p className="mt-1 text-xs text-[#526159] sm:text-sm">{duration}</p><p className="mt-2 text-[11px] leading-4 text-[#7b847e] sm:text-xs">{advice}</p></div>}
