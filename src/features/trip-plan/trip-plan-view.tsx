"use client";

import Link from "next/link";
import { useMemo, useState } from "react";
import type { DayPlan, TripInput, TripPlan } from "@/types/trip";
import { priorityOptions, travelStyles } from "@/features/trip-input/options";
import { DayFeedback } from "@/features/feedback/day-feedback";

const intensity = { easy: "轻松", moderate: "适中", intense: "充实" };
const method = { walk: "步行", public_transport: "公共交通", taxi: "打车", mixed: "组合交通" };
const mainTypes = new Set(["attraction","shopping","entertainment"]);

export function TripPlanView({ plan, input, onReviseDay, notice, canUndo, onUndo, canEdit, tripId }: { plan: TripPlan; input: TripInput; onReviseDay?: (day: DayPlan) => void; notice?: string[]; canUndo?: boolean; onUndo?: () => void; canEdit?: boolean; tripId?: string }) {
  const [copied,setCopied] = useState(false);
  const budgetLabel = plan.budget.estimateType === "exact" && plan.budget.estimatedTotal !== null ? `预计 ¥${plan.budget.estimatedTotal}` : plan.budget.estimateType === "range" && plan.budget.estimatedRange ? `参考 ¥${plan.budget.estimatedRange.min}–${plan.budget.estimatedRange.max}` : "按偏好灵活安排";
  const understanding = useMemo(() => {
    const counts = plan.days.map((day) => day.activities.filter((activity) => mainTypes.has(activity.type)).length);
    const average = counts.reduce((sum,value) => sum + value,0) / counts.length;
    const walking = plan.days.reduce((sum,day) => sum + day.estimatedWalkingKm,0) / plan.days.length;
    const methods = plan.days.flatMap((day) => day.activities.map((activity) => activity.transportToNext?.method).filter(Boolean));
    const transport = methods.length ? methods.sort((a,b) => methods.filter((value) => value === b).length - methods.filter((value) => value === a).length)[0]! : "mixed";
    return { mainActivities: `${Math.min(...counts)}–${Math.max(...counts)}个/天（平均${average.toFixed(1)}个）`, walking: walking <= 4 ? "较少" : walking <= 7 ? "适中" : "较多", transport: method[transport as keyof typeof method] };
  },[plan]);
  const style = travelStyles.find((item) => item.value === input.travelStyle);
  const priorities = input.priorities.map((value) => priorityOptions.find((item) => item.value === value)?.label).filter(Boolean);
  return <main className="pb-20">
    {!canEdit && <section className="border-b border-[#245b46]/10 bg-[#eaf2ed]"><div className="page-shell flex flex-col gap-3 py-5 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-bold text-[#245b46]">朋友邀请你一起看看这份旅行计划</p><p className="mt-1 text-sm text-[#65706a]">这是一份只读分享，不需要登录。</p></div><Link href="/#plan" className="shrink-0 rounded-full bg-[#245b46] px-5 py-2.5 text-center text-sm font-bold text-white">我也想规划一次</Link></div></section>}
    <section className="bg-[#204f3c] py-14 text-white sm:py-20"><div className="page-shell"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-sm font-semibold text-white/65">{plan.destination.city} · {plan.days.length}天</p><h1 className="mt-3 max-w-3xl text-4xl font-bold tracking-tight sm:text-6xl">这几天这样玩</h1><p className="mt-3 text-xl font-semibold text-white/85">{plan.title}</p></div>{tripId && <button onClick={async () => { await navigator.clipboard.writeText(window.location.href); setCopied(true); setTimeout(() => setCopied(false),1600); }} className="rounded-full bg-white/15 px-4 py-2 text-sm font-bold hover:bg-white/25">{copied ? "链接已复制 ✓" : "一起看看这份旅行计划"}</button>}</div><p className="mt-5 max-w-2xl leading-7 text-white/75">{plan.summary}</p></div></section>
    <section className="page-shell -mt-5 relative z-10"><div className="card rounded-3xl p-5 sm:p-8"><p className="text-sm font-bold text-[#287057]">这份计划为你这样安排</p><div className="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[
      ["你的选择", [style?.label,...priorities].filter(Boolean).join(" · ") || style?.label], ["整体节奏",plan.strategy.pace], ["主要活动",understanding.mainActivities], ["步行强度",understanding.walking], ["主要交通",understanding.transport], ["建议住在",plan.strategy.recommendedStayArea], ["预算模式",budgetLabel], ["为什么这样安排",plan.summary],
    ].map(([label,value]) => <div key={label} className={label === "为什么这样安排" ? "rounded-2xl bg-[#f4f6f1] p-4 sm:col-span-2" : "rounded-2xl bg-[#f4f6f1] p-4"}><p className="text-xs font-bold text-[#7b847e]">{label}</p><p className="mt-2 text-sm font-semibold leading-6">{value}</p></div>)}</div></div></section>
    {((notice?.length || canUndo)) && <div className="page-shell mt-6 flex flex-col gap-3 rounded-2xl border border-[#245b46]/15 bg-[#eaf2ed] p-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-bold text-[#245b46]">{canUndo ? "已调整当天行程" : "行程已恢复"}</p><p className="mt-1 text-sm text-[#53635b]">{notice?.length ? notice.join("；") : "最近一次修改已保存。"}</p></div>{canUndo && onUndo && <button onClick={onUndo} className="rounded-full border border-[#245b46]/20 bg-white px-4 py-2 text-sm font-bold text-[#245b46]">还是用刚才的安排</button>}</div>}
    <div className="page-shell mt-8 grid gap-6 lg:grid-cols-[1fr_300px]"><div className="space-y-6">{plan.days.map((day) => <article key={day.dayNumber} className="card overflow-hidden rounded-3xl"><header className="border-b border-black/5 p-5 sm:p-7"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-sm font-bold text-[#287057]">DAY {day.dayNumber}{day.date ? ` · ${day.date}` : ""}</p><h2 className="mt-2 text-2xl font-bold">{day.title}</h2><p className="mt-1 text-[#6f7973]">{day.theme}</p></div>{onReviseDay && <button onClick={() => onReviseDay(day)} className="rounded-full border border-black/10 bg-white px-4 py-2 text-sm font-semibold text-[#245b46]">今天不满意？告诉我怎么改</button>}</div><div className="mt-5 flex flex-wrap gap-4 text-xs font-semibold text-[#65706a]"><span>强度：{intensity[day.intensity]}</span><span>步行约 {day.estimatedWalkingKm} km</span>{day.estimatedCost !== null && <span>预计 ¥{day.estimatedCost}</span>}</div></header><div className="p-5 sm:p-7">{day.activities.map((activity,index) => <div key={activity.id} className="relative flex gap-4 pb-8 last:pb-0"><div className="flex w-12 shrink-0 flex-col items-center"><span className="text-xs font-bold text-[#287057]">{activity.startTime}</span>{index < day.activities.length - 1 && <span className="mt-2 h-full w-px bg-[#dbe4de]" />}</div><div className="min-w-0 flex-1"><div className="flex flex-wrap items-baseline justify-between gap-2"><h3 className="font-bold">{activity.name}</h3><span className="text-xs text-[#7b847e]">{activity.area} · {activity.durationMinutes}分钟</span></div><p className="mt-2 text-sm leading-6 text-[#65706a]">{activity.reason}</p>{activity.transportToNext && <p className="mt-3 rounded-xl bg-[#f2f5f0] px-3 py-2 text-xs text-[#566159]">下一站：{method[activity.transportToNext.method]}约 {activity.transportToNext.durationMinutes} 分钟 · {activity.transportToNext.description}</p>}</div></div>)}<div className="mt-5 rounded-2xl bg-[#fff7e9] p-4"><p className="text-sm font-bold text-[#9a6223]">今日提醒</p><ul className="mt-2 space-y-1 text-sm leading-6 text-[#785f41]">{day.dayTips.map((tip) => <li key={tip}>· {tip}</li>)}</ul></div>{tripId && <DayFeedback tripId={tripId} dayNumber={day.dayNumber} />}</div></article>)}</div><aside className="space-y-5 lg:sticky lg:top-6 lg:self-start"><section className="card rounded-3xl p-6"><h2 className="font-bold">住在哪里</h2><p className="mt-3 text-lg font-bold text-[#245b46]">{plan.strategy.recommendedStayArea}</p><p className="mt-2 text-sm leading-6 text-[#65706a]">{plan.strategy.stayReason}</p></section><section className="card rounded-3xl p-6"><h2 className="font-bold">预算概览</h2><strong className="mt-4 block text-xl">{budgetLabel}</strong><p className="mt-3 text-xs leading-5 text-[#7b847e]">{plan.budget.notes}</p></section><section className="rounded-3xl bg-[#f0e4cd] p-6"><h2 className="font-bold">出发前确认</h2><ul className="mt-3 space-y-2 text-sm leading-6 text-[#63553f]">{plan.generalTips.map((tip) => <li key={tip}>· {tip}</li>)}</ul></section></aside></div>
    <p className="page-shell mt-8 text-center text-xs leading-5 text-[#858d88]">{plan.dataDisclaimer}</p>
  </main>;
}
