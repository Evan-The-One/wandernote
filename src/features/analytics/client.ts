export function trackEvent(eventName:string,data:Record<string,unknown>={}){const body=JSON.stringify({eventName,...data});try{if(navigator.sendBeacon){navigator.sendBeacon("/api/analytics",new Blob([body],{type:"application/json"}));return;}void fetch("/api/analytics",{method:"POST",headers:{"content-type":"application/json"},body,keepalive:true});}catch{/* analytics never blocks product */}}
