import{and,eq}from"drizzle-orm";import{currentMiniappUser}from"@/server/auth/miniapp";import{getDatabase}from"@/server/database/client";import{posterPages,tripImageTasks,trips}from"@/server/database/schema";import{apiError,HttpError}from"@/server/http";import{createPosterDownloadUrl}from"@/server/posters/storage";
export async function POST(request:Request,{params}:{params:Promise<{taskId:string;pageId:string}>}){try{const user=await currentMiniappUser(request),{taskId,pageId}=await params;const[page]=await getDatabase().select({storageKey:posterPages.storageKey}).from(posterPages).innerJoin(tripImageTasks,eq(posterPages.posterTaskId,tripImageTasks.id)).innerJoin(trips,eq(tripImageTasks.tripId,trips.id)).where(and(eq(posterPages.id,pageId),eq(posterPages.posterTaskId,taskId),eq(posterPages.userId,user.id),eq(trips.userId,user.id))).limit(1);if(!page)throw new HttpError(404,"没有找到这张海报","POSTER_PAGE_NOT_FOUND");return Response.json(await createPosterDownloadUrl(page.storageKey),{headers:{"cache-control":"private, no-store"}})}catch(error){return apiError(error)}}
