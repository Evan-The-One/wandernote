import {redirect} from "next/navigation";import {currentUser} from "@/server/auth/user";import {AccountClient} from "@/features/account/account-client";
export const dynamic="force-dynamic";export default async function AccountPage({searchParams}:{searchParams:Promise<{tab?:string}>}){if(!await currentUser())redirect('/login?returnTo=/account');const {tab}=await searchParams;if(!tab)redirect('/account?tab=settings');return <main className="app-container max-w-5xl py-7 sm:py-12"><AccountClient contactEmail={process.env.PUBLIC_CONTACT_EMAIL||null}/></main>}
